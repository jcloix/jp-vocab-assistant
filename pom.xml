<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.jcloix.jpvocab</groupId>
    <artifactId>jp-vocab</artifactId>
    <version>0.1.0</version>
    <packaging>pom</packaging>

    <modules>
        <module>domain</module>
        <module>util</module>
        <module>aws-support</module>
        <module>persistence</module>
        <module>task-source</module>
        <module>ai-generation</module>
        <module>notification</module>
        <module>lambda-task-detector</module>
        <module>lambda-discord-interaction</module>
        <module>lambda-task-completion</module>
    </modules>

    <properties>
        <!-- Java -->
        <java.version>17</java.version>
        <maven.compiler.release>17</maven.compiler.release>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- AWS -->
        <aws.sdk.version>2.26.6</aws.sdk.version>
        <aws.lambda.core.version>1.2.3</aws.lambda.core.version>
        <aws.lambda.events.version>3.11.5</aws.lambda.events.version>

        <!-- Google -->
        <google.auth.version>1.26.0</google.auth.version>
        <google.docs.version>v1-rev20250917-2.0.0</google.docs.version>
        <google.api.client.version>2.8.1</google.api.client.version>
        <google.oauth.jetty.version>1.36.0</google.oauth.jetty.version>
        <google.http.gson.version>1.45.1</google.http.gson.version>
        <google.genai.version>1.32.0</google.genai.version>

        <!-- Utils -->
        <jackson.version>2.17.2</jackson.version>
        <slf4j.version>2.0.13</slf4j.version>
        <bouncycastle.version>1.70</bouncycastle.version>
        <commons.codec.version>1.15</commons.codec.version>
        <kuromoji.version>0.9.0</kuromoji.version>

        <!-- Test -->
        <junit.version>5.10.2</junit.version>
        <mockito.version>5.6.0</mockito.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>bom</artifactId>
                <version>${aws.sdk.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Internal modules -->
            <dependency>
                <groupId>io.github.jcloix.jpvocab</groupId>
                <artifactId>domain</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>io.github.jcloix.jpvocab</groupId>
                <artifactId>util</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>io.github.jcloix.jpvocab</groupId>
                <artifactId>task-source</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>io.github.jcloix.jpvocab</groupId>
                <artifactId>persistence</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>io.github.jcloix.jpvocab</groupId>
                <artifactId>ai-generation</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>io.github.jcloix.jpvocab</groupId>
                <artifactId>notification</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>io.github.jcloix.jpvocab</groupId>
                <artifactId>aws-support</artifactId>
                <version>${project.version}</version>
            </dependency>

            <dependency>
                <groupId>org.bouncycastle</groupId>
                <artifactId>bcprov-jdk18on</artifactId>
                <version>1.78</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- ================= -->
        <!-- TESTING           -->
        <!-- ================= -->

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>5.10.2</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>5.6.0</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <version>5.6.0</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Unit tests -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <excludedGroups>integration</excludedGroups>
                </configuration>
            </plugin>

            <!-- Integration tests -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>3.2.5</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
